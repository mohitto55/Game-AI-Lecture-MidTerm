#include <iostream>
#include <thread>
#include "CMiner.h"
#include "CEnterMineAndForNugget.h"
using namespace std;

int main()
{
    //CMiner miner("FMiner", CEnterMineAndForNugget::GetInstance());

    //for (int i = 0; i < 20; ++i) {
    //    miner.Update();
    //    this_thread::sleep_for(std::chrono::milliseconds(800));
    //}
    return 0;
}